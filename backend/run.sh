node server.js > logs/server.txt &
# or use nodemon for development

# for running it at startup first you have to navigate into this directory, and then run "run.sh" from here, bacause the node.js module paths are relative to where you run them from.